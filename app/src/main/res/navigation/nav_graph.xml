<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/counterNavHost">

    <activity
        android:id="@+id/counterNavHost"
        android:name="com.example.knitting.CounterNavHost"
        android:label="activity_counter_nav_host"
        tools:layout="@layout/activity_counter_nav_host" />
</navigation>